{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "npx lerna run start --stream",
    "setup": "npx lerna bootstrap",
    "setupdb": "npx lerna run setupdb"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
